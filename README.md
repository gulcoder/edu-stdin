# edu-stdin
## Beskrivning
Ett C++ projekt som tar emot namn via standardinput och h√§lsar an√§ndaren. 
Anv√§ndaren kan skriva "quit" for att avsluta programmet. 

##Installation 
1. Klona detta repository:

```bash 
git clone https://github.com/<ditt_github_anv√ndarnamn>/edu-stdin.git 
cd edu-stdin

```

2. Bygg projektet med CMake:

```bash
cmake -B build
make -C build

```

3. K√r programmet:
```bash
./bin/hello
```
